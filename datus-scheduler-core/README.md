# datus-scheduler-core

Core abstractions for Datus scheduler adapters: `BaseSchedulerAdapter`, data models, registry, and config base classes.

## Install

```bash
pip install datus-scheduler-core
```

## Usage

This package provides the base classes. Install a concrete adapter:

```bash
pip install datus-scheduler-airflow
```
