# docker-compose reads this file automatically if named `.env` in this dir.
# Copy to `.env` and fill in the password; `.env` is gitignored.
# Shell env vars override `.env`.

# Password for the seeded `starrocks_local` Airflow connection.
# User / host / port / database are pinned in docker-compose.yml.
STARROCKS_PASSWORD=Datus2025
